server:
  port: 8082

spring:
  application:
    name: aml-ingest

  artemis:
    mode: native
    broker-url: tcp://localhost:61616
    user: admin
    password: admin

aml:
  queues:
    bank-tx-in: BANK.TX.IN
    screening-tx: AML.SCREENING.TX
    bank-tx-in-dlq: BANK.TX.IN.DLQ

management:
  endpoints:
    web:
      exposure:
        include: health,info
